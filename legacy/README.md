# legacy/

Superseded artifacts kept for reference only — not part of the build.

- `pareto_revenue_impact.html`, `pareto_all_impacts_horizontal_dropdown.html` —
  static Plotly exports of the aftermarket/service Pareto. Replaced in M1 by the
  data-driven `paretoChart` factory (`src/charts/pareto/`) rendering the weighted
  impact from `src/data/aftermarket-pain-points.ts`.
